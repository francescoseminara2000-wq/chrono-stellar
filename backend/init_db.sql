CREATE DATABASE IF NOT EXISTS greengrocer;
USE greengrocer;

-- Utente di esempio per l'accesso (opzionale, se vuoi creare un utente specifico)
-- CREATE USER 'app_user'@'localhost' IDENTIFIED BY 'secure_password';
-- GRANT ALL PRIVILEGES ON greengrocer.* TO 'app_user'@'localhost';
-- FLUSH PRIVILEGES;
